{
  "name": "dr-toru",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "check": "tsc --noEmit && eslint --cache . && prettier --cache --check .",
    "check:fix": "tsc --noEmit && eslint --cache --fix . && prettier --cache --write .",
    "check:all": "tsc --noEmit && eslint . && prettier --check .",
    "check:fix-all": "tsc --noEmit && eslint --fix . && prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "package:medasr-plugin": "./scripts/package-medasr-plugin.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-opener": "^2.5.3",
    "onnxruntime-web": "^1.24.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@tauri-apps/cli": "^2",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "jsdom": "^28.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.6.2",
    "typescript-eslint": "^8.55.0",
    "vite": "^6.0.3",
    "vitest": "^4.0.18"
  }
}
